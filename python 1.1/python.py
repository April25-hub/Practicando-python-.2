def max(a, b):
    if a > b:
        return a  # Si 'a' es mayor que 'b', devuelve 'a'
    else:
        return b  # Si no, devuelve 'b'

#esta linea dara un jemplo de uso:
print(max(10, 20))  # Devuelve 20
print(max(50, 30))  # Devuelve 50
